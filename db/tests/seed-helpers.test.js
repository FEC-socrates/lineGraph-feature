const seedHelpers = require('../seed-helpers')

describe('randPrice', () => {
  test('test1', () => {
    expect(true).toEqual(true);
  });
});

// hoursType
// createRandomStock
// generateTicker
// createRandomCompany